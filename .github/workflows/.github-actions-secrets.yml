# This file documents the required secrets for GitHub Actions workflows
# Add these secrets in: Repository Settings → Secrets and variables → Actions

secrets:
  NETLIFY_AUTH_TOKEN:
    description: Netlify personal access token for deployment
    required: true
  NETLIFY_SITE_ID:
    description: Netlify site ID for the target deployment site
    required: true
